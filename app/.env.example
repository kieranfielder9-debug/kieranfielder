# The deployed backend's URL (see /server). Expo inlines any variable
# prefixed EXPO_PUBLIC_ into the app at build time — copy this file to
# .env and fill in the real value once the backend is deployed.
EXPO_PUBLIC_API_URL=http://localhost:4000
