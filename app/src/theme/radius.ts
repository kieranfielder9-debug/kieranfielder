export const radius = {
  /** Cards, panels, neomorphic surfaces. */
  md: 16,
  /** Search bar, small pills. */
  pill: 22,
  /** Avatars, icon buttons, the FAB. */
  full: 9999,
} as const;

export const borderWidth = {
  default: 1,
  /** The FAB's gold ring only. */
  accentRing: 3,
} as const;
